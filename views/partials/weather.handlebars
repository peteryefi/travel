<div class="weatherWidget container">
	{{#each partials.weather.locations}}
		<div class="locations">
			<h3 class="bg-info">{{name}}</h3>
			<a href="{{forecastUrl}}">
				<img src="{{iconUrl}}" alt="{{weather}}">
				{{weather}}, {{temp}}
			</a>
		</div>
	{{/each}}
	<small>Source: <a href="http://www.wunderground.com">Weather Underground</a></small>
</div>